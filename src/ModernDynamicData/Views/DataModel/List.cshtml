@using System.Threading.Tasks
@using ModernDynamicData
@using ModernDynamicData.ViewModels.DataModel
@model ListViewModel
<h1>Data Model liste</h1>
<ul>
    @foreach (var dataModel in Model.DataModels)
    {
        <li>
            <a asp-route="@Constants.Routes.ContextDetail" asp-route-dataModel="@dataModel.DataModelName">@dataModel.DataModelName</a>
        </li>
    }
</ul>